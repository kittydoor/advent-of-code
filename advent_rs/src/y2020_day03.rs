pub fn toboggan_trajectory(_contents: &str, part: &str) -> Result<String, String> {
    //let map = contents

    match part {
        //"p1" => Ok(toboggan_trajectory_p1(&map).to_string()),
        //"p2" => Ok(toboggan_trajectory_p2(&map).to_string()),
        _ => Err("<part> must be p1 or p2!".to_string()),
    }
}
